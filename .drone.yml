kind: pipeline
name: default
workspace:
  base: /tmp
  path: "."
steps:
  - name: build
    image: node
    commands:
      - npm install
      - npm install -g vuepress
      - vuepress build docs
    when:
      event: [ push, tag ]